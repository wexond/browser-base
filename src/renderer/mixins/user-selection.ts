const noUserSelect = () => `
  user-select: none;
`;

const noTapHighlight = () => `
  -webkit-tap-highlight-color:  rgba(255, 255, 255, 0) !important;
`;

export default {
  noUserSelect,
  noTapHighlight,
};
