import styled from 'styled-components';

export const Root = styled.div`
  height: 30px;
  display: inline-flex;
  padding-left: 12px;
  padding-right: 12px;
  border-radius: 16px;
  align-items: center;
  cursor: pointer;
  background-color: rgba(0, 0, 0, 0.12);
`;
