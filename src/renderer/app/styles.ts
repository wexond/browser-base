import { css } from 'styled-components';

export const Style = css`
  body {
    user-select: none;
    cursor: default;
    margin: 0;
    background-color: white;
    padding: 0;
    width: 100vw;
    height: 100vh;
    overflow: hidden;
  }
`;
