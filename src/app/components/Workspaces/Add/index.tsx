import React from 'react';

import { Root, Icon } from './styles';

export default class extends React.Component<{}, {}> {
  public render() {
    return (
      <Root>
        <Icon className="icon" />
      </Root>
    );
  }
}
