export enum Platforms {
  Windows = 'win32',
  MacOS = 'darwin',
  Linux = 'linux',
}

export enum MenuItems {
  TabGroups = 'tabGroups',
  History = 'history',
  Extensions = 'extensions',
  Bookmarks = 'bookmarks',
  Settings = 'settings',
  About = 'about',
}
